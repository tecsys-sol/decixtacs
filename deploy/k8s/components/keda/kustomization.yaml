# KEDA autoscaling of the collect workers on the Celery queue length in Redis.
# Requires KEDA (keda.sh) >= 2.12.
apiVersion: kustomize.config.k8s.io/v1alpha1
kind: Component
resources:
  - scaledobject.yaml
