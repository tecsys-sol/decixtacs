# Base URL of the API as seen by the browser. Leave empty to use same-origin requests (/api/v1/...).
NEXT_PUBLIC_API_URL=
# Backend used by the Next.js rewrite for /api/* (dev default: http://localhost:8000).
API_PROXY_TARGET=http://localhost:8000
